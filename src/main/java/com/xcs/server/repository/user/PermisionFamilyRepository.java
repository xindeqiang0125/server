package com.xcs.server.repository.user;

import com.xcs.server.domain.user.PermissionFamily;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PermisionFamilyRepository extends JpaRepository<PermissionFamily,Integer> {
}
